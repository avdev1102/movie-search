module.exports = {
    darkMode: "class", // Enable class-based dark mode
    content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"], // Specify files to scan for classes
    theme: {
      extend: {}, // Extend Tailwind's default theme (optional)
    },
    plugins: [], // Add Tailwind plugins (optional)
  };